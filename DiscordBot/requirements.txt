# Image To Sight worker runtime. Keep this list limited to imports used by
# tools/vtracer_worker.py; benchmark-only packages are not production runtime dependencies.
Pillow==11.3.0
numpy==2.2.6
vtracer==0.6.15
